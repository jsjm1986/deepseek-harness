# 插件安全与可控热插拔开发计划

## 目标

本计划规定 DSH 如何支持员工创作插件和灵活热插拔，同时不把 AI 审查当作安全边界。安全目标是防止不受信任的插件获得其所有者没有明确授予的主机权限。

DSH 已经通过 Cordis 插件组合运行时行为，并通过 effect 撤销注册，还可以按会话组合 Agent Preset。本计划在现有组合模型外增加信任分级和准入控制。

## 范围

本计划覆盖可能被作者以外的用户使用的插件完整生命周期。

- 员工创作和测试插件。
- 审查源代码、依赖、构建产物和声明的能力。
- 批准、签名、发布、分配和撤销。
- 运行时能力执行和审计记录。
- 热加载、热卸载、版本变化和卸载失败处理。

本计划不假设进程内 JavaScript 插件仅因为通过 DSH 插件注册表暴露，就自动获得隔离。

## 信任级别

| 级别 | 适用内容 | 运行位置 | 批准方式 | 热插拔策略 |
| --- | --- | --- | --- | --- |
| 声明式 | 配置、Schema、UI 描述和不可执行的提示词数据 | 主机进程，但不允许任意代码钩子 | 自动检查 | 内容验证后允许 |
| 隔离式 | 使用批准插件 SDK 的员工代码 | 独立 Worker、进程、容器或远程执行器 | AI 审查加策略检查；提升能力需要人工审查 | 只允许加载已批准产物 |
| 受信宿主式 | 必须在 Cordis 内运行的核心扩展和代码 | Gateway 进程 | 管理员安全批准 | 受限；敏感变更应用于新的进程或会话 |

员工可以创建隔离式插件，但不能因此获得安装受信宿主式插件的权限。批准发布插件不等于获得生产使用权限。

## 准入流程

1. 将审查对象冻结为源文件、锁文件、传递依赖、安装脚本、生成产物、原生模块、配置和不可变内容摘要。
2. 要求插件声明能力清单，覆盖文件系统目录、子进程、网络目的地、凭据、会话和模型上下文、动态代码、原生代码以及跨用户数据。
3. 对错误元数据、未声明入口、依赖策略违规、密钥和禁止的宿主 API 执行确定性检查。
4. 让 AI 审查员比较声明的能力和实际观察到的行为，并生成带源代码位置证据的发现报告。
5. 在隔离环境中运行产物，将实际观察到的文件、进程、网络和凭据访问与能力清单比较。
6. 根据请求的能力和信任级别，将结果交给自动批准、团队审查或管理员安全批准。
7. 对批准的产物签名，并将签名、摘要、能力授权、审查决定和过期策略绑定到发布版本。

生产加载器必须拒绝未签名产物、已变化的依赖闭包、缺少清单的插件，或用户策略未授予的能力请求。

## 权限模型

授权模型将插件所有权与插件执行权限分开。

- `author` 创建和更新由员工或团队拥有的草稿。
- `test` 只能在隔离开发环境运行草稿。
- `review` 查看证据，并批准插件的风险级别。
- `publish` 对已批准的不可变产物签名并提升版本。
- `grant` 将已批准产物及其能力分配给用户、团队或会话。
- `execute` 只能运行当前策略分配给调用者的产物。

策略引擎必须评估完整的插件解析图，包括别名、Preset、传递依赖，以及通过 RPC 或命令行入口加载的插件。

## AI 审查职责

AI 审查员是分析服务，不是授予执行权限的最终主体。

- 它接收的插件数据是不受信任的输入，不能执行代码注释、文档或字符串中嵌入的指令。
- 它不具备写权限、非受限网络权限、密钥，或执行被提交插件的权限。
- 它的报告记录实际观察到的能力、未声明访问、数据流风险、权限提升路径、依赖问题、生命周期清理风险，以及带证据的置信度。
- 缺少 AI 报告或报告置信度不足时，不能自动批准高权限插件。
- 当确定性策略检查或运行时执行与 AI 报告冲突时，以确定性检查和运行时执行为准。

## 热插拔规则

1. 热加载只接受已经批准、已签名、不可变且摘要与授权记录一致的产物。
2. 任何源文件、依赖、构建产物、清单或配置变化都会产生需要重新审查的新产物。
3. 当能力已经获得授权时，声明式和隔离式插件可以按会话加载；受信宿主式插件必须遵守宿主级策略。
4. 撤销时停止隔离 Worker、撤销注册、使能力令牌失效，并阻止新会话加载该产物。
5. 敏感权限变化在下一次会话或进程启动时生效，除非运行时能够证明所有现有注册和能力都已移除。

## 开发阶段

1. 确定威胁模型、信任级别、能力词汇、审批角色，以及 AI 服务不可用时的处理方式。
2. 在 profile、Preset、Gateway 和运行时准入位置增加插件元数据和策略评估，对缺少或含义不明确的权限采用拒绝加载。
3. 提供隔离插件执行路径和能力代理，使员工代码无法获得 Gateway 进程环境。
4. 建立不可变产物、确定性扫描器、AI 证据报告、隔离运行轨迹、签名和依赖来源记录组成的审查流程。
5. 增加热插拔生命周期、撤销、审计记录、会话快照，以及卸载失败和部分注册的测试。
6. 将现有插件迁移到对应信任级别，并在下列验收条件通过且本机生产服务器验证后再启用生产部署。

## 验收标准

- 缺少有效能力清单的插件在加载前被拒绝。
- 未声明的文件系统、子进程、网络、凭据或跨用户能力被拒绝或由运行时阻断。
- 普通用户不能加载受信宿主式插件，也不能授予高权限能力。
- 源文件或任意依赖发生变化后，原有批准和签名失效。
- 员工可以创建和测试隔离式插件，但无法访问生产密钥或其他用户的无关工作区。
- 撤销后不能新加载插件，并且活动隔离插件的执行路径会被终止。
- 热卸载会移除插件拥有的每个工具、服务、事件监听器、Worker 和能力令牌。
- 审计记录包含用户、插件摘要、请求能力、决定、审查证据、加载时间和撤销时间。

## 不在本计划的内容

- 将 AI 分数或成功的源代码审查当作插件安全证明。
- 在多用户生产部署中允许任意员工代码运行在 Gateway 进程内。
- 只使用 UI 开关或模型指令作为唯一执行控制。
- 承诺所有安全敏感权限变化都能在已经运行的受信宿主进程内立即生效。

## 参考资料

- [架构说明](docs/architecture.md)介绍插件组合、可撤销 effect 和扩展点。
- [Agent Preset](packages/preset/README.md)介绍按会话组合及其所有权规则。
- [插件清单](packages/host/plugin-inventory/README.md)介绍当前插件发现和清单界面。
- [防御性模式](docs/defensive-patterns.md)介绍生命周期、子进程、并发和卸载要求。
