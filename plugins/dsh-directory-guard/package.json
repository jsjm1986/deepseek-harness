{
  "name": "@deepseek-ai/dsh-directory-guard",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "In-dsh directory permission guard: denies fs tool calls outside a user's granted roots.",
  "main": "lib/index.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "*",
    "@deepseek-ai/dsh-tools": "*"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.22.4",
    "typescript": "^5.9.2",
    "vitest": "^4.1.8"
  },
  "files": [
    "lib",
    "cordis.admin.patch.yml",
    "cordis.patch.yml",
    "README.md",
    "README.zh.md"
  ]
}
